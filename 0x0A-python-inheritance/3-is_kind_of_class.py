#!/usr/bin/python3
"""Defines a function to check instances
of object."""


def is_kind_of_class(obj, a_class):
    """returns True if object is instnace else
    returns False.
    """

    return (isinstance(obj, a_class))
